You can only run the tests if you have sourced the GMXRC file.
    
Note that there are separate single and double precision
reference values, so you need to use the -double option
to the test script when checking a double precision build.
