This is the same kind of setup as nbnxn_vsite, but replicated with

gmx genconf -box 1 2 1 -f ../nbnxn_vsite/conf.gro

so that by default mdrun will test DD using more ranks in the Y direction.

Re-generated solvent box, re-equilibrated, doubled max-mpi-ranks and
adapted [molecules] accordingly.
